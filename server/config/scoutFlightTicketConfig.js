/**
 * Scout Flight Tournament Ticket — earned via Perk Machine spin progress.
 * Adjust SPINS_PER_TOURNAMENT_TICKET to rebalance earn rate.
 */

/** Every N Perk Machine spins awards one Tournament Ticket. */
const SPINS_PER_TOURNAMENT_TICKET = 10;

module.exports = {
  SPINS_PER_TOURNAMENT_TICKET,
};
